<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.petsource">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/paw"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SigninActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".PetListActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".AddPetActivity"
            android:label="@string/title_activity_add_pet"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PetSalonActivity"
            android:label="@string/title_activity_pet_salon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PetCareActivity"
            android:label="@string/title_activity_pet_care"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".UpdateAccActivity"
            android:label="@string/title_activity_update_acc"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>